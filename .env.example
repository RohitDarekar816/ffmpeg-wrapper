# Host port the API is published on (host side). Container always listens on 8000.
HOST_PORT=8000

# Base URL used to build the returned MP3 link.
# Same Docker network as n8n  -> http://ffmpeg-api:8000
# Public server / reverse proxy -> https://media.yourdomain.com
PUBLIC_BASE_URL=http://ffmpeg-api:8000
